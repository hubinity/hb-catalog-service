# Contract Requirements Checklist: Schema de resposta da operação de leitura de saldo

**Purpose**: Validar a qualidade, clareza e completude dos requisitos do spec da T-001-4 (reescrita do schema StockItem + content da '200') antes da revisão de PR e do `/speckit-plan`. Testa o que está **escrito** no spec — não a implementação.
**Created**: 2026-07-23
**Feature**: [spec.md](../spec.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements.

## Requirement Completeness

- [x] CHK001 O schema-alvo está completo no spec — bloco YAML com todos os campos, tipos, formats, `minimum`, `required` e descriptions por propriedade? [Completeness, Spec §Decisão de escopo, §FR-001]
- [x] CHK002 O texto exato da nova `description` do Path Item está fixado (FR-004), sem redação livre? [Completeness, Spec §FR-004]
- [x] CHK003 A estrutura exata do `content` da `'200'` está definida (media type `application/json` + `$ref`, espelhando `getProductById`)? [Completeness, Spec §FR-002]
- [x] CHK004 A description do próprio schema (`Current stock balance snapshot...`) está especificada, substituindo a antiga? [Completeness, Spec §Decisão de escopo]

## Requirement Clarity

- [x] CHK005 O critério de "paridade nome + tipo equivalente" com o record do serviço está mapeado objetivamente (UUID↔uuid, Integer↔int32, Instant↔date-time)? [Measurability, Spec §US1, §SC-001]
- [x] CHK006 A exclusão de `updatedAt` do `required` está justificada com o comportamento real do serviço (nulo antes da primeira mudança) e o critério de inclusão dos demais 4 está claro (inicializados no INSERT)? [Clarity, Spec §Edge Cases, §Assumptions]
- [x] CHK007 "Regressão zero no consumidor" está definida operacionalmente (`mvn -B verify` verde com artefato reinstalado)? [Clarity, Spec §FR-006]
- [x] CHK008 O `minimum: 0` dos contadores está fundamentado (invariante do domínio: contadores nunca negativos) sem introduzir validação além do que o serviço garante? [Clarity, Spec §FR-001, §Edge Cases]

## Requirement Consistency

- [x] CHK009 FR-003 (0 ocorrências dos campos antigos) é consistente com FR-005 (mudança restrita) — a remoção acontece integralmente dentro da fronteira declarada (schema `StockItem`)? [Consistency, Spec §FR-003, §FR-005]
- [x] CHK010 A decisão "reusar `StockItem`" está reconciliada com o rótulo `StockItemResponse` do tracker — o spec registra explicitamente que o rótulo nomeia a entrega, não o schema? [Consistency, Spec §Decisões do usuário]
- [x] CHK011 O edge case "nome do DTO gerado" é consistente com Out of Scope (convergência DTO gerado × record local atribuída a T-004-x)? [Consistency, Spec §Edge Cases, §Out of Scope]
- [x] CHK012 FR-004 (pendência restante = só T-001-5) é consistente com o estado real da cadeia (T-001-1..3 entregues) e com a progressão de descriptions das features anteriores? [Consistency, Spec §FR-004]

## Acceptance Criteria Quality

- [x] CHK013 Cada cenário de aceitação da US1 rastreia para um FR (cenário 1 ↔ FR-002, cenário 2 ↔ FR-001, cenário 3 ↔ FR-005/SC-002)? [Traceability, Spec §US1]
- [x] CHK014 SC-003 é objetivamente mensurável (grep = 0 ocorrências + verify verde), sem termos vagos? [Measurability, Spec §SC-003]
- [x] CHK015 SC-004 (T-001-5 desbloqueada) está documentado como efeito externo (TASKS.json) fora do controle deste spec? [Dependency, Spec §SC-004]

## Scenario & Edge Case Coverage

- [x] CHK016 O modo de falha da breaking change está coberto — se `mvn -B verify` do consumidor falhar por referência ao DTO antigo, a correção do ponto de uso entra nesta entrega (com registro da baixa probabilidade verificada)? [Edge Case, Spec §Edge Cases, §FR-006]
- [x] CHK017 A exclusão do corpo de erro do `'404'` (ProblemDetail) está documentada como decisão consciente com justificativa (padrão transversal do serviço, fora da cadeia)? [Coverage, Spec §Assumptions, §Out of Scope]
- [x] CHK018 A diferença de tipo int64 (contrato antigo) → int32 (serviço real) está registrada como parte consciente do alinhamento? [Coverage, Spec §Edge Cases]

## Dependencies & Assumptions

- [x] CHK019 As 2 decisões estruturais do usuário estão registradas com data e origem (confirmação do pipeline, 2026-07-23), fechando o achado L2 e a divergência da feature 001? [Assumption, Spec §Decisões do usuário, §Assumptions]
- [x] CHK020 A segurança da mudança breaking está amparada por referência verificável (ADR 0006 — build-only, sem consumidores publicados) + prova empírica (FR-006)? [Assumption, Spec §Assumptions]
- [x] CHK021 A fonte de verdade dos campos (`api/dto/StockItemResponse.java` do serviço) está referenciada com caminho verificável? [Dependency, Spec §Contexto técnico]

## Notes

- Check items off as completed: `[x]`
- Itens com `[Gap]`/`[Ambiguity]` procedentes devem gerar edição do spec antes do `/speckit-plan`
- Escopo acordado: somente T-001-4; profundidade padrão; audiência: revisor de PR (herdados da cadeia)
- Implementação validada no commit contratual `8e0f218`: OpenAPI 3.1 preservado, schema `StockItem` com 5 campos em paridade, campos antigos ausentes e `'200'` referenciando o schema.
- DTO regenerado: `UUID productId`, `Integer available`, `Integer reserved`, `Integer reorderPoint` obrigatórios e `OffsetDateTime updatedAt` opcional; consumidor sem referências diretas e com 201 testes verdes.
