# Contract Requirements Checklist: Requisito de autorização da operação de leitura de saldo

**Purpose**: Validar a qualidade, clareza e completude dos requisitos do spec da T-001-5 (securityScheme + security global no contrato) antes da revisão de PR e do `/speckit-plan`. Testa o que está **escrito** no spec — não a implementação.
**Created**: 2026-07-24
**Feature**: [spec.md](../spec.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements.

## Requirement Completeness

- [x] CHK001 O bloco `securitySchemes/bearerAuth` está completo no spec (type, scheme, bearerFormat, description)? [Completeness, Spec §Decisão de escopo, §FR-001]
- [x] CHK002 O bloco `security` global está especificado com valor exato (`- bearerAuth: []`) e posição (nível raiz)? [Completeness, Spec §Decisão de escopo, §FR-002]
- [x] CHK003 O texto exato da nova `description` do Path Item (estado final) está fixado, sem redação livre? [Completeness, Spec §FR-004]
- [x] CHK004 A `description` do próprio `bearerAuth` está especificada (identifica emissor Keycloak realm hibit sem URL)? [Completeness, Spec §FR-001]

## Requirement Clarity

- [x] CHK005 "Herda o requisito global" (FR-003) é inequívoco — o spec explicita que a ausência de `security` na operação é intencional (não omissão)? [Clarity, Spec §FR-003, §US1]
- [x] CHK006 "Autenticação, não admin" está mapeado objetivamente ao serviço (`/api/**` authenticated + reads sem @PreAuthorize)? [Measurability, Spec §SC-003]
- [x] CHK007 A escolha de `security` global (vs. por operação) tem critério claro para o revisor julgar (todas as operações do contrato exigem auth no serviço)? [Clarity, Spec §Assumptions]
- [x] CHK008 "bearerFormat: JWT é informativo/documentacional" está explicitado, evitando expectativa de validação pelo campo? [Clarity, Spec §Edge Cases]

## Requirement Consistency

- [x] CHK009 FR-002 (security global aplica a todas) é consistente com o Edge Case "getProductById passa a exigir auth" — o spec reconhece e aceita esse efeito colateral? [Consistency, Spec §FR-002, §Edge Cases]
- [x] CHK010 FR-003 (operação sem security próprio) é consistente com FR-005 (mudança restrita) — a operação não é editada, o requisito vem do raiz? [Consistency, Spec §FR-003, §FR-005]
- [x] CHK011 A não-modelagem de admin nas mutações (Out of Scope) é consistente com o security global bearerAuth — o spec deixa claro que "autenticado" é piso, e reforço de role é task futura? [Consistency, Spec §Edge Cases, §Out of Scope]
- [x] CHK012 A não-modelagem de exceções permitAll é consistente com o fato de esses endpoints não constarem no contrato? [Consistency, Spec §Edge Cases]

## Acceptance Criteria Quality

- [x] CHK013 Cada cenário de aceitação da US1 rastreia para um FR (cenário 1 ↔ FR-001/FR-002, cenário 2 ↔ FR-003, cenário 3 ↔ FR-005/SC-002)? [Traceability, Spec §US1]
- [x] CHK014 SC-003 (requisito corresponde ao serviço) é verificável objetivamente (comparação com SecurityConfig)? [Measurability, Spec §SC-003]
- [x] CHK015 SC-004 (cadeia T-001 completa) é verificável — enumera os 5 elementos da operação (endereço, verbo, parâmetro, corpo, autorização)? [Measurability, Spec §SC-004]

## Scenario & Edge Case Coverage

- [x] CHK016 O modo de falha "security global sobrescreve indevidamente uma operação que deveria ser pública" está coberto — o spec confirma que nenhuma operação do contrato é permitAll no serviço? [Edge Case, Spec §Edge Cases]
- [x] CHK017 A fronteira read vs. mutação está coberta — o spec atribui o reforço de admin das mutações a task futura, sem deixá-lo implícito no security global? [Coverage, Spec §Edge Cases, §Out of Scope]
- [x] CHK018 A decisão de não modelar OAuth2/OIDC flows está documentada com justificativa (roles vêm de realm_access, não de scopes)? [Coverage, Spec §Assumptions, §Out of Scope]

## Dependencies & Assumptions

- [x] CHK019 As 2 decisões estruturais do usuário estão registradas com data e origem (confirmação do pipeline, 2026-07-24)? [Assumption, Spec §Decisões do usuário]
- [x] CHK020 A fonte de verdade (`config/SecurityConfig.java`) está referenciada com a regra efetiva citada (permitAll/hasRole/authenticated/denyAll)? [Dependency, Spec §Contexto técnico]
- [x] CHK021 O encerramento da cadeia T-001 está declarado como marco (SC-004), com verificação de que nenhuma outra task de contrato da operação resta? [Assumption, Spec §SC-004]

## Notes

- Check items off as completed: `[x]`
- Itens com `[Gap]`/`[Ambiguity]` procedentes devem gerar edição do spec antes do `/speckit-plan`
- Escopo acordado: somente T-001-5; profundidade padrão; audiência: revisor de PR (herdados da cadeia)
- Decisão sem precedente interno (primeiro securityScheme do contrato) — atenção redobrada do revisor em CHK007/CHK009
- Implementação validada no commit contratual `68873d5`: OpenAPI 3.1 preservado, `bearerAuth` HTTP/JWT global sem scopes ou roles, e operação de saldo sem `security` próprio.
- Fidelidade confirmada contra `/api/**.authenticated()` e ausência de `@PreAuthorize` nos reads; reactor Maven verde e consumidor com 201 testes sem falhas.
