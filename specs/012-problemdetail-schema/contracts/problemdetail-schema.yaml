# Fragmento-alvo de T-002-7-1 — NÃO é um documento OpenAPI completo.
#
# Arquivo destino: platform-shared-contracts/contracts-catalog/openapi/catalog.yaml
# Seção destino: components/schemas — anexar ao final, após ProductImageResponse
#
# MUDANÇA ESTRITAMENTE ADITIVA — zero remoções, como em T-002-1, T-002-3 e T-002-4.
#
# Escopo: apenas a declaração do schema. Nenhum desfecho de erro é referenciado
# aqui — isso é T-002-7-2 (getProductById 404), T-002-7-3 (getStockItemByProductId
# 404), T-002-7-4 (addProductImage 400), T-002-7-5 (addProductImage 403) e
# T-002-7-6 (addProductImage 404).
#
# Forma transcrita de org.springframework.http.ProblemDetail, já usado em
# runtime por com.hubinity.catalog.api.error.ApiExceptionHandler (verificado
# em código — ver research.md R1).

# ============================================================================
# ANEXAR em `components/schemas`, APÓS `ProductImageResponse` (último da
# lista hoje). Indentação: 4 espaços.
# ============================================================================

    ProblemDetail:
      type: object
      description: |
        RFC 7807 problem details object. Every error response from this API
        uses this shape, produced by the service's global exception handler.
        All members are optional per RFC 7807; a member absent from a given
        response was not set by the handler that produced it.
      properties:
        type:
          type: string
          format: uri
          description: |
            A URI identifying the problem type. "about:blank" when the
            handler did not set a more specific type.
        title:
          type: string
          description: A short, human-readable summary of the problem type.
        status:
          type: integer
          format: int32
          description: The HTTP status code generated by the origin server.
        detail:
          type: string
          description: A human-readable explanation specific to this occurrence.
        instance:
          type: string
          format: uri
          description: A URI identifying the specific occurrence of the problem.
